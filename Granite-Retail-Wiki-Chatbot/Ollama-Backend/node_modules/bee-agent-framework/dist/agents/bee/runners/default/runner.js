import { BaseRunner } from '../base.js';
import { Retryable } from '../../../../internals/helpers/retryable.js';
import { AgentError } from '../../../base.js';
import { BeeUserEmptyPrompt, BeeUserPrompt, BeeSystemPrompt, BeeAssistantPrompt, BeeToolErrorPrompt, BeeToolInputErrorPrompt, BeeToolNoResultsPrompt, BeeToolNotFoundPrompt, BeeSchemaErrorPrompt } from '../../prompts.js';
import { Tool, ToolInputValidationError, ToolError } from '../../../../tools/base.js';
import { FrameworkError } from '../../../../errors.js';
import { isTruthy, last, isEmpty } from 'remeda';
import { LinePrefixParser, LinePrefixParserError } from '../../../parsers/linePrefix.js';
import { ZodParserField, JSONParserField } from '../../../parsers/field.js';
import { z } from 'zod';
import { Role, BaseMessage } from '../../../../llms/primitives/message.js';
import { TokenMemory } from '../../../../memory/tokenMemory.js';
import { getProp } from '../../../../internals/helpers/object.js';
import { Cache } from '../../../../cache/decoratorCache.js';
import { shallowCopy } from '../../../../serializer/utils.js';

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
function _ts_decorate(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
  else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
__name(_ts_decorate, "_ts_decorate");
function _ts_metadata(k, v) {
  if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
}
__name(_ts_metadata, "_ts_metadata");
class DefaultRunner extends BaseRunner {
  static {
    __name(this, "DefaultRunner");
  }
  static {
    this.register();
  }
  async llm({ signal, meta, emitter }) {
    const tempMessageKey = "tempMessage";
    return new Retryable({
      onRetry: /* @__PURE__ */ __name(() => emitter.emit("retry", {
        meta
      }), "onRetry"),
      onError: /* @__PURE__ */ __name(async (error) => {
        await emitter.emit("error", {
          error,
          meta
        });
        this.failedAttemptsCounter.use(error);
        if (error instanceof LinePrefixParserError) {
          if (error.reason === LinePrefixParserError.Reason.NoDataReceived) {
            await this.memory.add(BaseMessage.of({
              role: Role.ASSISTANT,
              text: "\n",
              meta: {
                [tempMessageKey]: true
              }
            }));
          } else {
            await this.memory.add(BaseMessage.of({
              role: Role.ASSISTANT,
              text: this.templates.schemaError.render({}),
              meta: {
                [tempMessageKey]: true
              }
            }));
          }
        }
      }, "onError"),
      executor: /* @__PURE__ */ __name(async () => {
        const tools = this.input.tools.slice();
        await emitter.emit("start", {
          meta,
          tools,
          memory: this.memory
        });
        const { parser, parserRegex } = this.createParser(tools);
        const llmOutput = await this.input.llm.generate(this.memory.messages.slice(), {
          signal,
          stream: true,
          guided: {
            regex: parserRegex.source
          }
        }).observe((llmEmitter) => {
          parser.emitter.on("update", async ({ value, key, field }) => {
            if (key === "tool_output" && parser.isDone) {
              return;
            }
            await emitter.emit("update", {
              data: parser.finalState,
              update: {
                key,
                value: field.raw,
                parsedValue: value
              },
              meta: {
                success: true,
                ...meta
              },
              memory: this.memory
            });
          });
          parser.emitter.on("partialUpdate", async ({ key, delta, value }) => {
            await emitter.emit("partialUpdate", {
              data: parser.finalState,
              update: {
                key,
                value: delta,
                parsedValue: value
              },
              meta: {
                success: true,
                ...meta
              }
            });
          });
          llmEmitter.on("newToken", async ({ value, callbacks }) => {
            if (parser.isDone) {
              callbacks.abort();
              return;
            }
            await parser.add(value.getTextContent());
            if (parser.partialState.tool_output !== void 0) {
              callbacks.abort();
            }
          });
        });
        await parser.end();
        await this.memory.deleteMany(this.memory.messages.filter((msg) => getProp(msg.meta, [
          tempMessageKey
        ]) === true));
        return {
          state: parser.finalState,
          raw: llmOutput
        };
      }, "executor"),
      config: {
        maxRetries: this.options.execution?.maxRetriesPerStep,
        signal
      }
    }).get();
  }
  async tool({ state, signal, meta, emitter }) {
    const tool = this.input.tools.find((tool2) => tool2.name.trim().toUpperCase() == state.tool_name?.trim()?.toUpperCase());
    if (!tool) {
      this.failedAttemptsCounter.use(new AgentError(`Agent was trying to use non-existing tool "${state.tool_name}"`, [], {
        context: {
          state,
          meta
        }
      }));
      return {
        success: false,
        output: this.templates.toolNotFoundError.render({
          tools: this.input.tools
        })
      };
    }
    return new Retryable({
      config: {
        signal,
        maxRetries: this.options.execution?.maxRetriesPerStep
      },
      onError: /* @__PURE__ */ __name(async (error) => {
        await emitter.emit("toolError", {
          data: {
            iteration: state,
            tool,
            input: state.tool_input,
            options: this.options,
            error: FrameworkError.ensure(error)
          },
          meta
        });
        this.failedAttemptsCounter.use(error);
      }, "onError"),
      executor: /* @__PURE__ */ __name(async () => {
        const toolOptions = shallowCopy(this.options);
        try {
          await emitter.emit("toolStart", {
            data: {
              tool,
              input: state.tool_input,
              options: toolOptions,
              iteration: state
            },
            meta
          });
          const toolOutput = await tool.run(state.tool_input, toolOptions).context({
            [Tool.contextKeys.Memory]: this.memory
          });
          await emitter.emit("toolSuccess", {
            data: {
              tool,
              input: state.tool_input,
              options: toolOptions,
              result: toolOutput,
              iteration: state
            },
            meta
          });
          if (toolOutput.isEmpty()) {
            return {
              output: this.templates.toolNoResultError.render({}),
              success: true
            };
          }
          return {
            success: true,
            output: toolOutput.getTextContent()
          };
        } catch (error) {
          await emitter.emit("toolError", {
            data: {
              tool,
              input: state.tool_input,
              options: toolOptions,
              error,
              iteration: state
            },
            meta
          });
          if (error instanceof ToolInputValidationError) {
            this.failedAttemptsCounter.use(error);
            return {
              success: false,
              output: this.templates.toolInputError.render({
                reason: error.toString()
              })
            };
          }
          if (error instanceof ToolError) {
            this.failedAttemptsCounter.use(error);
            return {
              success: false,
              output: this.templates.toolError.render({
                reason: error.explain()
              })
            };
          }
          throw error;
        }
      }, "executor")
    }).get();
  }
  get renderers() {
    const self = {
      user: {
        message: /* @__PURE__ */ __name(({ prompt }) => prompt !== null || this.input.memory.isEmpty() ? BaseMessage.of({
          role: Role.USER,
          text: prompt || this.templates.userEmpty.render({}),
          meta: {
            createdAt: /* @__PURE__ */ new Date()
          }
        }) : void 0, "message")
      },
      system: {
        variables: {
          tools: /* @__PURE__ */ __name(async () => {
            return await Promise.all(this.input.tools.map(async (tool) => ({
              name: tool.name,
              description: tool.description.replaceAll("\n", ".").replace(/\.$/, "").concat("."),
              schema: JSON.stringify(await tool.getInputJsonSchema(), /* @__PURE__ */ (() => {
                const ignoredKeys = /* @__PURE__ */ new Set([
                  "minLength",
                  "maxLength",
                  "$schema"
                ]);
                return (key, value) => ignoredKeys.has(key) ? void 0 : value;
              })())
            })));
          }, "tools")
        },
        message: /* @__PURE__ */ __name(async () => BaseMessage.of({
          role: Role.SYSTEM,
          text: this.templates.system.render({
            tools: await self.system.variables.tools(),
            instructions: void 0,
            createdAt: (/* @__PURE__ */ new Date()).toISOString()
          }),
          meta: {
            createdAt: /* @__PURE__ */ new Date()
          }
        }), "message")
      }
    };
    return self;
  }
  async initMemory({ prompt }) {
    const { memory: history, llm } = this.input;
    const prevConversation = [
      ...history.messages,
      this.renderers.user.message({
        prompt
      })
    ].filter(isTruthy).map((message) => {
      if (message.role === Role.USER) {
        const isEmpty2 = !message.text.trim();
        const text = isEmpty2 ? (this.templates?.userEmpty ?? BeeUserEmptyPrompt).render({}) : (this.templates?.user ?? BeeUserPrompt).render({
          input: message.text,
          meta: {
            ...message?.meta,
            createdAt: message?.meta?.createdAt?.toISOString?.()
          }
        });
        return BaseMessage.of({
          role: Role.USER,
          text,
          meta: message.meta
        });
      }
      return message;
    });
    const memory = new TokenMemory({
      llm,
      capacityThreshold: 0.85,
      syncThreshold: 0.5,
      handlers: {
        removalSelector(curMessages) {
          const prevConversationMessage = curMessages.find((msg) => prevConversation.includes(msg));
          if (prevConversationMessage && prevConversationMessage !== last(prevConversation)) {
            return prevConversationMessage;
          }
          const lastMessage = curMessages.length > 3 ? curMessages.find((msg) => msg.role === Role.ASSISTANT && getProp(msg, [
            "ctx",
            "success"
          ]) === false) ?? curMessages.find((msg) => msg.role === Role.ASSISTANT) : null;
          if (!lastMessage) {
            throw new AgentError("Cannot fit the current conversation into the context window!");
          }
          return lastMessage;
        }
      }
    });
    await memory.addMany([
      await this.renderers.system.message(),
      ...prevConversation
    ]);
    return memory;
  }
  get templates() {
    const customTemplates = this.input.templates ?? {};
    return {
      system: customTemplates.system ?? BeeSystemPrompt,
      assistant: customTemplates.assistant ?? BeeAssistantPrompt,
      user: customTemplates.user ?? BeeUserPrompt,
      userEmpty: customTemplates.userEmpty ?? BeeUserEmptyPrompt,
      toolError: customTemplates.toolError ?? BeeToolErrorPrompt,
      toolInputError: customTemplates.toolInputError ?? BeeToolInputErrorPrompt,
      toolNoResultError: customTemplates.toolNoResultError ?? BeeToolNoResultsPrompt,
      toolNotFoundError: customTemplates.toolNotFoundError ?? BeeToolNotFoundPrompt,
      schemaError: customTemplates.schemaError ?? BeeSchemaErrorPrompt
    };
  }
  createParser(tools) {
    const parserRegex = isEmpty(tools) ? new RegExp(`Thought: .+\\nFinal Answer: [\\s\\S]+`) : new RegExp(`Thought: .+\\n(?:Final Answer: [\\s\\S]+|Function Name: (${tools.map((tool) => tool.name).join("|")})\\nFunction Input: \\{.*\\}\\nFunction Output:)`);
    const parser = new LinePrefixParser({
      thought: {
        prefix: "Thought:",
        next: [
          "tool_name",
          "final_answer"
        ],
        isStart: true,
        field: new ZodParserField(z.string().min(1))
      },
      tool_name: {
        prefix: "Function Name:",
        next: [
          "tool_input"
        ],
        field: new ZodParserField(z.pipeline(z.string().trim(), z.enum(tools.map((tool) => tool.name))))
      },
      tool_input: {
        prefix: "Function Input:",
        next: [
          "tool_output"
        ],
        isEnd: true,
        field: new JSONParserField({
          schema: z.object({}).passthrough(),
          base: {},
          matchPair: [
            "{",
            "}"
          ]
        })
      },
      tool_output: {
        prefix: "Function Output:",
        next: [
          "final_answer"
        ],
        isEnd: true,
        field: new ZodParserField(z.string())
      },
      final_answer: {
        prefix: "Final Answer:",
        next: [],
        isStart: true,
        isEnd: true,
        field: new ZodParserField(z.string().min(1))
      }
    }, {
      waitForStartNode: true,
      endOnRepeat: true,
      fallback: /* @__PURE__ */ __name((stash) => stash ? [
        {
          key: "thought",
          value: "I now know the final answer."
        },
        {
          key: "final_answer",
          value: stash
        }
      ] : [], "fallback")
    });
    return {
      parser,
      parserRegex
    };
  }
}
_ts_decorate([
  Cache({
    enumerable: false
  }),
  _ts_metadata("design:type", void 0),
  _ts_metadata("design:paramtypes", [])
], DefaultRunner.prototype, "renderers", null);
_ts_decorate([
  Cache({
    enumerable: false
  }),
  _ts_metadata("design:type", typeof BeeAgentTemplates === "undefined" ? Object : BeeAgentTemplates),
  _ts_metadata("design:paramtypes", [])
], DefaultRunner.prototype, "templates", null);

export { DefaultRunner };
//# sourceMappingURL=runner.js.map
//# sourceMappingURL=runner.js.map