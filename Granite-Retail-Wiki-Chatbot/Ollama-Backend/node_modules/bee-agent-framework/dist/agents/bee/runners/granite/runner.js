import { BaseMessage, Role } from '../../../../llms/primitives/message.js';
import { isEmpty } from 'remeda';
import { DefaultRunner } from '../default/runner.js';
import { GraniteBeeUserPrompt, GraniteBeeSystemPrompt, GraniteBeeAssistantPrompt, GraniteBeeSchemaErrorPrompt, GraniteBeeToolNotFoundPrompt, GraniteBeeToolErrorPrompt, GraniteBeeToolInputErrorPrompt } from './prompts.js';
import { Cache } from '../../../../cache/decoratorCache.js';

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
function _ts_decorate(decorators, target, key, desc) {
  var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
  if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
  else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
  return c > 3 && r && Object.defineProperty(target, key, r), r;
}
__name(_ts_decorate, "_ts_decorate");
function _ts_metadata(k, v) {
  if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
}
__name(_ts_metadata, "_ts_metadata");
class GraniteRunner extends DefaultRunner {
  static {
    __name(this, "GraniteRunner");
  }
  static {
    this.register();
  }
  constructor(input, options, run) {
    super(input, options, run);
    run.emitter.on("update", async ({ update, meta, memory }) => {
      if (update.key === "tool_output") {
        await memory.add(BaseMessage.of({
          role: "tool_response",
          text: update.value,
          meta: {
            success: meta.success
          }
        }));
      }
    }, {
      isBlocking: true
    });
  }
  async initMemory(input) {
    const memory = await super.initMemory(input);
    if (!isEmpty(this.input.tools)) {
      const index = memory.messages.findIndex((msg) => msg.role === Role.SYSTEM) + 1;
      await memory.add(BaseMessage.of({
        role: "tools",
        text: JSON.stringify((await this.renderers.system.variables.tools()).map((tool) => ({
          name: tool.name,
          description: tool.description,
          schema: JSON.parse(tool.schema)
        })), null, 4)
      }), index);
    }
    return memory;
  }
  get templates() {
    const customTemplates = this.input.templates ?? {};
    return {
      ...super.templates,
      user: customTemplates.user ?? GraniteBeeUserPrompt,
      system: customTemplates.system ?? GraniteBeeSystemPrompt,
      assistant: customTemplates.assistant ?? GraniteBeeAssistantPrompt,
      schemaError: customTemplates.schemaError ?? GraniteBeeSchemaErrorPrompt,
      toolNotFoundError: customTemplates.toolNotFoundError ?? GraniteBeeToolNotFoundPrompt,
      toolError: customTemplates.toolError ?? GraniteBeeToolErrorPrompt,
      toolInputError: customTemplates.toolInputError ?? GraniteBeeToolInputErrorPrompt
    };
  }
  createParser(tools) {
    const { parser } = super.createParser(tools);
    return {
      parserRegex: isEmpty(tools) ? new RegExp(`Thought: .+\\nFinal Answer: [\\s\\S]+`) : new RegExp(`Thought: .+\\n(?:Final Answer: [\\s\\S]+|Tool Name: (${tools.map((tool) => tool.name).join("|")})\\nTool Input: \\{.*\\})`),
      parser: parser.fork((nodes, options) => ({
        options,
        nodes: {
          ...nodes,
          thought: {
            ...nodes.thought,
            prefix: "Thought:"
          },
          tool_name: {
            ...nodes.tool_name,
            prefix: "Tool Name:"
          },
          tool_input: {
            ...nodes.tool_input,
            prefix: "Tool Input:",
            isEnd: true,
            next: []
          },
          final_answer: {
            ...nodes.final_answer,
            prefix: "Final Answer:"
          }
        }
      }))
    };
  }
}
_ts_decorate([
  Cache({
    enumerable: false
  }),
  _ts_metadata("design:type", typeof BeeAgentTemplates === "undefined" ? Object : BeeAgentTemplates),
  _ts_metadata("design:paramtypes", [])
], GraniteRunner.prototype, "templates", null);

export { GraniteRunner };
//# sourceMappingURL=runner.js.map
//# sourceMappingURL=runner.js.map