'use strict';

var serializable_cjs = require('../../internals/serializable.cjs');
var utils_cjs = require('../../serializer/utils.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
const Role = {
  ASSISTANT: "assistant",
  SYSTEM: "system",
  USER: "user"
};
class BaseMessage extends serializable_cjs.Serializable {
  static {
    __name(this, "BaseMessage");
  }
  role;
  text;
  meta;
  constructor(role, text, meta) {
    super(), this.role = role, this.text = text, this.meta = meta;
  }
  static {
    this.register();
  }
  static of({ role, text, meta }) {
    return new BaseMessage(role, text, meta);
  }
  createSnapshot() {
    return {
      role: this.role,
      text: this.text,
      meta: utils_cjs.shallowCopy(this.meta)
    };
  }
  loadSnapshot(state) {
    return Object.assign(this, state);
  }
}

exports.BaseMessage = BaseMessage;
exports.Role = Role;
//# sourceMappingURL=message.cjs.map
//# sourceMappingURL=message.cjs.map