'use strict';

var base_cjs = require('../../llms/base.cjs');
var remeda = require('remeda');
var llm_cjs = require('../../llms/llm.cjs');
var emitter_cjs = require('../../emitter/emitter.cjs');
var utils_cjs = require('../../serializer/utils.cjs');
var errors_cjs = require('../../errors.cjs');
var object_cjs = require('../../internals/helpers/object.cjs');
var client_cjs = require('./client.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
function isGrpcServiceError(err) {
  return err instanceof Error && err.constructor.name === "Error" && "code" in err && typeof err.code === "number";
}
__name(isGrpcServiceError, "isGrpcServiceError");
class IBMvLLMOutput extends base_cjs.BaseLLMOutput {
  static {
    __name(this, "IBMvLLMOutput");
  }
  text;
  meta;
  constructor(text, meta) {
    super(), this.text = text, this.meta = meta;
  }
  static {
    this.register();
  }
  merge(other) {
    this.text += other.text;
    object_cjs.assign(this.meta, other.meta);
  }
  getTextContent() {
    return this.text;
  }
  toString() {
    return this.getTextContent();
  }
  createSnapshot() {
    return {
      text: this.text,
      meta: utils_cjs.shallowCopy(this.meta)
    };
  }
  loadSnapshot(snapshot) {
    Object.assign(this, snapshot);
  }
}
class IBMvLLM extends llm_cjs.LLM {
  static {
    __name(this, "IBMvLLM");
  }
  emitter = new emitter_cjs.Emitter({
    namespace: [
      "ibm_vllm",
      "llm"
    ],
    creator: this
  });
  client;
  parameters;
  constructor({ client, modelId, parameters = {}, executionOptions, cache }) {
    super(modelId, executionOptions, cache);
    this.client = client ?? new client_cjs.Client();
    this.parameters = parameters ?? {};
  }
  static {
    this.register();
  }
  async meta() {
    const response = await this.client.modelInfo({
      model_id: this.modelId
    });
    return {
      tokenLimit: response.max_sequence_length
    };
  }
  async embed(input, { chunkSize, signal, ...options } = {}) {
    const results = await Promise.all(remeda.chunk(input, chunkSize ?? 100).map(async (texts) => {
      const response = await this.client.embed({
        model_id: this.modelId,
        truncate_input_tokens: options?.truncate_input_tokens ?? 512,
        texts
      }, {
        signal
      });
      const embeddings = response.results?.vectors.map((vector) => {
        const embedding = vector[vector.data]?.values;
        if (!embedding) {
          throw new base_cjs.LLMError("Missing embedding");
        }
        return embedding;
      });
      if (embeddings?.length !== texts.length) {
        throw new base_cjs.LLMError("Missing embedding");
      }
      return embeddings;
    }));
    return {
      embeddings: results.flat()
    };
  }
  async tokenize(input) {
    try {
      const response = await this.client.tokenize({
        model_id: this.modelId,
        requests: [
          {
            text: input
          }
        ]
      });
      const output = response.responses.at(0);
      if (!output) {
        throw new base_cjs.LLMError("Missing output", [], {
          context: {
            response
          }
        });
      }
      return {
        tokens: output.tokens,
        tokensCount: output.token_count
      };
    } catch (err) {
      throw this._transformError(err);
    }
  }
  async _generate(input, options, run) {
    try {
      const response = await this.client.generate({
        model_id: this.modelId,
        requests: [
          {
            text: input
          }
        ],
        params: this._prepareParameters(options)
      }, {
        signal: run.signal
      });
      const output = response.responses.at(0);
      if (!output) {
        throw new base_cjs.LLMError("Missing output", [], {
          context: {
            response
          }
        });
      }
      const { text, ...rest } = output;
      return new IBMvLLMOutput(text, rest);
    } catch (err) {
      throw this._transformError(err);
    }
  }
  async *_stream(input, options, run) {
    try {
      const stream = await this.client.generateStream({
        model_id: this.modelId,
        request: {
          text: input
        },
        params: this._prepareParameters(options)
      }, {
        signal: run.signal
      });
      for await (const chunk2 of stream) {
        const typedChunk = chunk2;
        const { text, ...rest } = typedChunk;
        if (text.length > 0) {
          yield new IBMvLLMOutput(text, rest);
        }
      }
    } catch (err) {
      throw this._transformError(err);
    }
  }
  createSnapshot() {
    return {
      ...super.createSnapshot(),
      client: this.client,
      modelId: this.modelId,
      parameters: utils_cjs.shallowCopy(this.parameters),
      executionOptions: utils_cjs.shallowCopy(this.executionOptions)
    };
  }
  loadSnapshot(snapshot) {
    super.loadSnapshot(snapshot);
    Object.assign(this, snapshot);
  }
  _transformError(error) {
    if (error instanceof errors_cjs.FrameworkError) {
      throw error;
    }
    if (isGrpcServiceError(error)) {
      throw new base_cjs.LLMError("LLM has occurred an error!", [
        error
      ], {
        isRetryable: [
          8,
          4,
          14
        ].includes(error.code)
      });
    }
    return new base_cjs.LLMError("LLM has occurred an error!", [
      error
    ]);
  }
  _prepareParameters(overrides) {
    const guided = object_cjs.omitUndefined(overrides?.guided ? {} : this.parameters.decoding ?? {});
    const guidedOverride = object_cjs.omitUndefined(overrides?.guided ?? {});
    if (guidedOverride?.choice) {
      guided.choice = {
        ...guided.choice,
        choices: guidedOverride.choice
      };
    } else if (guidedOverride?.grammar) {
      guided.grammar = guidedOverride.grammar;
    } else if (guidedOverride?.json) {
      guided.json_schema = remeda.isString(guidedOverride.json) ? guidedOverride.json : JSON.stringify(guidedOverride.json);
    } else if (guidedOverride?.regex) {
      guided.regex = guidedOverride.regex;
    } else if (!remeda.isEmpty(guidedOverride ?? {})) {
      throw new errors_cjs.NotImplementedError(`Following types ${Object.keys(overrides.guided).join(",")}" for the constraint decoding are not supported!`);
    }
    return {
      ...this.parameters,
      decoding: guided
    };
  }
}

exports.IBMvLLM = IBMvLLM;
exports.IBMvLLMOutput = IBMvLLMOutput;
//# sourceMappingURL=llm.cjs.map
//# sourceMappingURL=llm.cjs.map