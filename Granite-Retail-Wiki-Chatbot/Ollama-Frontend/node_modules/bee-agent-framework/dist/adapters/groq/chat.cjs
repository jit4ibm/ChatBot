'use strict';

var utils_cjs = require('../../serializer/utils.cjs');
var chat_cjs = require('../../llms/chat.cjs');
var message_cjs = require('../../llms/primitives/message.cjs');
var emitter_cjs = require('../../emitter/emitter.cjs');
var groqSdk = require('groq-sdk');
var serializer_cjs = require('../../serializer/serializer.cjs');
var object_cjs = require('../../internals/helpers/object.cjs');

var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
class ChatGroqOutput extends chat_cjs.ChatLLMOutput {
  static {
    __name(this, "ChatGroqOutput");
  }
  responses;
  constructor(response) {
    super();
    this.responses = [
      response
    ];
  }
  static {
    this.register();
  }
  get messages() {
    return this.responses.flatMap((response) => response.choices).flatMap((choice) => message_cjs.BaseMessage.of({
      role: choice.delta.role,
      text: choice.delta.content
    }));
  }
  getTextContent() {
    return this.messages.map((msg) => msg.text).join("\n");
  }
  merge(other) {
    this.responses.push(...other.responses);
  }
  toString() {
    return this.getTextContent();
  }
  createSnapshot() {
    return {
      responses: utils_cjs.shallowCopy(this.responses)
    };
  }
  loadSnapshot(snapshot) {
    Object.assign(this, snapshot);
  }
}
class GroqChatLLM extends chat_cjs.ChatLLM {
  static {
    __name(this, "GroqChatLLM");
  }
  emitter = emitter_cjs.Emitter.root.child({
    namespace: [
      "groq",
      "chat_llm"
    ],
    creator: this
  });
  client;
  parameters;
  constructor({ client, modelId = "llama-3.1-70b-versatile", parameters = {
    temperature: 0
  }, executionOptions = {}, cache } = {}) {
    super(modelId, executionOptions, cache);
    this.client = client ?? new groqSdk.Groq();
    this.parameters = parameters ?? {};
  }
  static {
    this.register();
    serializer_cjs.Serializer.register(groqSdk.Groq, {
      toPlain: /* @__PURE__ */ __name((value) => ({
        options: object_cjs.getPropStrict(value, "_options")
      }), "toPlain"),
      fromPlain: /* @__PURE__ */ __name((value) => new groqSdk.Groq(value.options), "fromPlain")
    });
  }
  async meta() {
    if (this.modelId.includes("gemma") || this.modelId.includes("llama3") || this.modelId.includes("llama-guard")) {
      return {
        tokenLimit: 8 * 1024
      };
    } else if (this.modelId.includes("llava-v1.5")) {
      return {
        tokenLimit: 4 * 1024
      };
    } else if (this.modelId.includes("llama-3.1-70b") || this.modelId.includes("llama-3.1-8b")) {
      return {
        tokenLimit: 128 * 1024
      };
    } else if (this.modelId.includes("mixtral-8x7b")) {
      return {
        tokenLimit: 32 * 1024
      };
    }
    return {
      tokenLimit: Infinity
    };
  }
  async embed(input, options) {
    const { data } = await this.client.embeddings.create({
      model: this.modelId,
      input: input.flatMap((msgs) => msgs.map((msg) => msg.text)),
      encoding_format: "float"
    }, {
      signal: options?.signal,
      stream: false
    });
    return {
      embeddings: data.map(({ embedding }) => embedding)
    };
  }
  async tokenize(input) {
    const contentLength = input.reduce((acc, msg) => acc + msg.text.length, 0);
    return {
      tokensCount: Math.ceil(contentLength / 4)
    };
  }
  _prepareRequest(input, options) {
    return {
      ...this.parameters,
      model: this.modelId,
      stream: false,
      messages: input.map((message) => ({
        role: message.role,
        content: message.text
      })),
      ...options?.guided?.json && {
        response_format: {
          type: "json_object"
        }
      }
    };
  }
  async _generate(input, options, run) {
    const response = await this.client.chat.completions.create({
      ...this._prepareRequest(input, options),
      stream: false
    }, {
      signal: run.signal
    });
    return new ChatGroqOutput({
      id: response.id,
      model: response.model,
      created: response.created,
      system_fingerprint: response.system_fingerprint,
      choices: response.choices.map((choice) => ({
        delta: choice.message,
        index: choice.index,
        logprobs: choice.logprobs,
        finish_reason: choice.finish_reason
      }))
    });
  }
  async *_stream(input, options, run) {
    for await (const chunk of await this.client.chat.completions.create({
      ...this._prepareRequest(input, options),
      stream: true
    }, {
      signal: run.signal
    })) {
      yield new ChatGroqOutput(chunk);
    }
  }
  createSnapshot() {
    return {
      ...super.createSnapshot(),
      parameters: utils_cjs.shallowCopy(this.parameters),
      client: this.client
    };
  }
}

exports.ChatGroqOutput = ChatGroqOutput;
exports.GroqChatLLM = GroqChatLLM;
//# sourceMappingURL=chat.cjs.map
//# sourceMappingURL=chat.cjs.map